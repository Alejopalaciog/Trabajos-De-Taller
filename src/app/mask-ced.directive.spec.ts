import { MaskCedDirective } from './mask-ced.directive';

describe('MaskCedDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskCedDirective();
    expect(directive).toBeTruthy();
  });
});
