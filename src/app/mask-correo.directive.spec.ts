import { MaskCorreoDirective } from './mask-correo.directive';

describe('MaskCorreoDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskCorreoDirective();
    expect(directive).toBeTruthy();
  });
});
