import { MaskMontoDirective } from './mask-monto.directive';

describe('MaskMontoDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskMontoDirective();
    expect(directive).toBeTruthy();
  });
});
