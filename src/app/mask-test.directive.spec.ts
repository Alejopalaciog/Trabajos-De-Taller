import { MaskTestDirective } from './mask-test.directive';

describe('MaskTestDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskTestDirective();
    expect(directive).toBeTruthy();
  });
});
